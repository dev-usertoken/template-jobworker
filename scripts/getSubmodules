git submodule update --init --recursive

( cd minerjobs-dev && git submodule init && git submodule update && git checkout master && git pull )
( cd minerjobs-dev/utils && git submodule init && git submodule update && git checkout master && git pull )
